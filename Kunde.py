class Kunde:
    def __init__(self, vorname, nachname, alter, geschlecht, adresse, konto):
        self.vorname = vorname  # Store the first name
        self.nachname = nachname  # Store the last name
        self.alter = alter  # Store the age
        self.geschlecht = geschlecht  # Store the gender
        self.adresse = adresse  # Store the address
        self.konto = konto  # Store the account
